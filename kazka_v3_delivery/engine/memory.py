"""
Component 6 — Memory: Persistence

The long-term memory system backed by the Obsidian vault structure.
Memory handles:
    - Storing generated assets with metadata
    - Tracking generation history and quality scores
    - Maintaining character consistency references
    - Persisting project state across sessions
    - Indexing the vault for fast retrieval

The vault structure (vault/) is the physical backing store.
Memory provides the API to read/write/query it.
"""

# TODO: Implement
# - Obsidian vault reader/writer (markdown + YAML frontmatter)
# - Asset metadata indexing
# - Character reference image management
# - Session state persistence
# - Generation history logging
