"""
Phase 2 — Parser: Tokens → Abstract Syntax Tree (AST)

Transforms the token stream from the lexer into a structured AST that
represents the Film Genome Document's hierarchical structure. Each strand
becomes a subtree with typed nodes.

Usage:
    Called by the compiler pipeline after the lexer phase.
"""

# TODO: Implement in Week 2
# - Define AST node types (StrandNode, FieldNode, ArrayNode, etc.)
# - Build tree from token stream
# - Resolve references (beat_id, character_id) into pointers
